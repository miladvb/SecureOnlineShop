@model SOSH.Web.Models.Product

<div class="col-4">
    <div class="card">
        <img src="~/Images/@(Model.ImageName)" class="card-img-top img-thumbnail" style="max-height: 300px;" />
        <div class="card-body">
            <h5 class="card-title">@Html.Raw(Model.Name)</h5>
            <a asp-action="Detail" asp-controller="Home" asp-route-ItemId="@Model.Id">ِDetail</a>
        </div>
    </div>
</div>